// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:970c6923937fc8bc3f5e11f951fb964a31913af8685ab7eb5d775288f9aacaa4
// Re-generate with: alef generate
#![allow(missing_docs)]
#![allow(deprecated, dead_code, unused_imports, unused_variables)]
#![allow(
    clippy::default_trait_access,
    clippy::cast_possible_wrap,
    clippy::cast_possible_truncation,
    clippy::cast_sign_loss,
    clippy::just_underscores_and_digits,
    clippy::unused_unit,
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::too_many_arguments,
    clippy::map_identity,
    clippy::unnecessary_cast,
    clippy::unwrap_or_default,
    clippy::derivable_impls,
    clippy::redundant_field_names,
    clippy::needless_borrows_for_generic_args,
    clippy::unnecessary_fallible_conversions,
    clippy::useless_conversion,
    clippy::arc_with_non_send_sync,
    clippy::collapsible_if,
    clippy::clone_on_copy,
    clippy::should_implement_trait
)]
#![allow(
    clippy::unsafe_derive_deserialize,
    clippy::must_use_candidate,
    clippy::return_self_not_must_use,
    clippy::use_self,
    clippy::missing_const_for_fn,
    clippy::missing_errors_doc,
    clippy::needless_pass_by_value,
    clippy::doc_markdown,
    clippy::derive_partial_eq_without_eq,
    clippy::uninlined_format_args,
    clippy::redundant_clone,
    clippy::implicit_clone,
    clippy::redundant_closure_for_method_calls,
    clippy::wildcard_imports,
    clippy::option_if_let_else,
    clippy::too_many_lines
)]
#![allow(clippy::multiple_unsafe_ops_per_block)]
#![allow(unsafe_code)]

use pyo3::prelude::*;
use std::collections::HashMap;
use std::sync::Arc;

/// Wrapper for trait visitor types (`Py<PyAny>`) that implements Clone.
///
/// `Py<PyAny>` is not Clone. This wrapper uses `Arc<Py<PyAny>>` internally for cheap cloning.
/// The .inner field is public for compatibility with generated code that needs to access
/// the underlying `Py<PyAny>` for trait dispatch.
#[derive(Debug)]
pub struct PyVisitorRef {
    pub inner: std::sync::Arc<pyo3::Py<pyo3::PyAny>>,
}

impl Clone for PyVisitorRef {
    fn clone(&self) -> Self {
        PyVisitorRef { inner: std::sync::Arc::clone(&self.inner) }
    }
}

impl From<pyo3::Py<pyo3::PyAny>> for PyVisitorRef {
    fn from(visitor: pyo3::Py<pyo3::PyAny>) -> Self {
        PyVisitorRef { inner: std::sync::Arc::new(visitor) }
    }
}

impl<'a, 'py> pyo3::FromPyObject<'a, 'py> for PyVisitorRef {
    type Error = pyo3::PyErr;

    fn extract(ob: pyo3::Borrowed<'a, 'py, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        Ok(PyVisitorRef { inner: std::sync::Arc::new(ob.to_owned().unbind()) })
    }
}

impl<'py> pyo3::conversion::IntoPyObject<'py> for PyVisitorRef {
    type Target = pyo3::PyAny;
    type Output = pyo3::Bound<'py, pyo3::PyAny>;
    type Error = std::convert::Infallible;

    fn into_pyobject(self, py: pyo3::Python<'py>) -> Result<Self::Output, Self::Error> {
        Ok((*self.inner).bind(py).clone())
    }
}

mod alef_json_str {
    use serde::{Deserialize, Deserializer};
    use serde_json::Value;
    pub fn deserialize<'de, D>(deserializer: D) -> Result<String, D::Error>
    where
        D: Deserializer<'de>,
    {
        let v = Value::deserialize(deserializer)?;
        Ok(match v {
            Value::String(s) => s,
            other => other.to_string(),
        })
    }
}

mod alef_json_str_opt {
    use serde::{Deserialize, Deserializer};
    use serde_json::Value;
    pub fn deserialize<'de, D>(deserializer: D) -> Result<Option<String>, D::Error>
    where
        D: Deserializer<'de>,
    {
        let v: Option<Value> = Option::deserialize(deserializer)?;
        Ok(v.and_then(|val| match val {
            Value::Null => None,
            Value::String(s) => Some(s),
            other => Some(other.to_string()),
        }))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ByteRange {
    #[pyo3(get)]
    pub start_byte: usize,
    #[pyo3(get)]
    pub end_byte: usize,
}

#[pymethods]
impl ByteRange {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (start_byte, end_byte))]
    #[new]
    pub fn new(start_byte: usize, end_byte: usize) -> Self {
        Self { start_byte, end_byte }
    }

    #[pyo3(signature = ())]
    pub fn is_valid(&self) -> bool {
        let core_self = structuredmerge_core::ByteRange {
            start_byte: self.start_byte,

            end_byte: self.end_byte,
        };
        core_self.is_valid()
    }

    #[pyo3(signature = ())]
    pub fn len(&self) -> usize {
        let core_self = structuredmerge_core::ByteRange {
            start_byte: self.start_byte,

            end_byte: self.end_byte,
        };
        core_self.len()
    }

    #[pyo3(signature = ())]
    pub fn is_empty(&self) -> bool {
        let core_self = structuredmerge_core::ByteRange {
            start_byte: self.start_byte,

            end_byte: self.end_byte,
        };
        core_self.is_empty()
    }

    #[pyo3(signature = (offset))]
    pub fn contains_byte(&self, offset: usize) -> bool {
        let core_self = structuredmerge_core::ByteRange {
            start_byte: self.start_byte,

            end_byte: self.end_byte,
        };
        core_self.contains_byte(offset)
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::ByteRange>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize)]
#[pyclass(frozen, from_py_object)]
pub struct ChildEdge {
    #[pyo3(get)]
    pub node_id: String,
    #[pyo3(get)]
    pub index: u64,
    #[pyo3(get)]
    pub field_name: Option<String>,
    #[pyo3(get)]
    pub extra: HashMap<String, String>,
}
impl<'de> serde::Deserialize<'de> for ChildEdge {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <structuredmerge_core::parsed::ChildEdge as serde::Deserialize>::deserialize(deserializer)
            .map(Into::into)
    }
}

#[pymethods]
impl ChildEdge {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (node_id, index, extra, field_name=None))]
    #[new]
    pub fn new(
        node_id: String,
        index: u64,
        extra: HashMap<String, String>,
        field_name: Option<String>,
    ) -> Self {
        Self { node_id, index, field_name, extra }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::parsed::ChildEdge>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize)]
#[pyclass(frozen, from_py_object)]
pub struct ConflictAlternative {
    #[pyo3(get)]
    pub revision: SourceRevision,
    #[pyo3(get)]
    pub state: ConflictAlternativeState,
    #[pyo3(get)]
    pub regions: Vec<OwnedSourceRegion>,
}
impl<'de> serde::Deserialize<'de> for ConflictAlternative {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <structuredmerge_core::ConflictAlternative as serde::Deserialize>::deserialize(deserializer)
            .map(Into::into)
    }
}

#[pymethods]
impl ConflictAlternative {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (revision, state, regions))]
    #[new]
    pub fn new(
        revision: SourceRevision,
        state: ConflictAlternativeState,
        regions: Vec<OwnedSourceRegion>,
    ) -> Self {
        Self { revision, state, regions }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::ConflictAlternative>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CoreError {
    #[pyo3(get)]
    pub code: String,
    #[pyo3(get)]
    pub message: String,
}

#[pymethods]
impl CoreError {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (code, message))]
    #[new]
    pub fn new(code: String, message: String) -> Self {
        Self { code, message }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::CoreError>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CoreParseResult {
    #[pyo3(get)]
    pub schema: String,
    #[pyo3(get)]
    pub selection: SelectionReport,
    #[pyo3(get)]
    pub backend: ParserProviderDescriptor,
    #[pyo3(get)]
    pub parsed: ParseOutput,
}

#[pymethods]
impl CoreParseResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (schema, selection, backend, parsed))]
    #[new]
    pub fn new(
        schema: String,
        selection: SelectionReport,
        backend: ParserProviderDescriptor,
        parsed: ParseOutput,
    ) -> Self {
        Self { schema, selection, backend, parsed }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::CoreParseResult>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct Diagnostic {
    #[pyo3(get)]
    pub severity: DiagnosticSeverity,
    #[pyo3(get)]
    pub category: DiagnosticCategory,
    #[pyo3(get)]
    pub message: String,
    #[pyo3(get)]
    pub path: Option<String>,
    #[pyo3(get)]
    pub review: Option<ReviewDiagnosticDetail>,
}

#[pymethods]
impl Diagnostic {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (severity, category, message, path=None, review=None))]
    #[new]
    pub fn new(
        severity: DiagnosticSeverity,
        category: DiagnosticCategory,
        message: String,
        path: Option<String>,
        review: Option<ReviewDiagnosticDetail>,
    ) -> Self {
        Self { severity, category, message, path, review }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::Diagnostic>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct LineEndings {
    #[pyo3(get)]
    pub lf: u64,
    #[pyo3(get)]
    pub crlf: u64,
    #[pyo3(get)]
    pub bare_cr: u64,
}

impl Default for LineEndings {
    fn default() -> Self {
        <structuredmerge_core::source::LineEndings as Default>::default().into()
    }
}

#[pymethods]
impl LineEndings {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (lf=Self::default().lf, crlf=Self::default().crlf, bare_cr=Self::default().bare_cr))]
    #[new]
    pub fn new(lf: u64, crlf: u64, bare_cr: u64) -> Self {
        Self { lf, crlf, bare_cr }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::source::LineEndings>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize)]
#[pyclass(frozen, from_py_object)]
pub struct MergeConflict {
    #[pyo3(get)]
    pub conflict_id: String,
    #[pyo3(get)]
    pub category: String,
    #[pyo3(get)]
    pub path: String,
    #[pyo3(get)]
    pub fallback_scope: String,
    #[pyo3(get)]
    pub message: String,
    #[pyo3(get)]
    pub alternatives: Vec<ConflictAlternative>,
}
impl<'de> serde::Deserialize<'de> for MergeConflict {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <structuredmerge_core::MergeConflict as serde::Deserialize>::deserialize(deserializer)
            .map(Into::into)
    }
}

#[pymethods]
impl MergeConflict {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (conflict_id, category, path, fallback_scope, message, alternatives))]
    #[new]
    pub fn new(
        conflict_id: String,
        category: String,
        path: String,
        fallback_scope: String,
        message: String,
        alternatives: Vec<ConflictAlternative>,
    ) -> Self {
        Self { conflict_id, category, path, fallback_scope, message, alternatives }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::MergeConflict>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize)]
#[pyclass(frozen, from_py_object)]
pub struct NativeExtension {
    #[pyo3(get)]
    pub schema: String,
    #[pyo3(get)]
    pub namespace: String,
    #[pyo3(get)]
    pub capabilities: Vec<String>,
    #[pyo3(get)]
    #[serde(default, deserialize_with = "alef_json_str::deserialize")]
    pub payload: String,
    #[pyo3(get)]
    pub extra: HashMap<String, String>,
}
impl<'de> serde::Deserialize<'de> for NativeExtension {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <structuredmerge_core::parsed::NativeExtension as serde::Deserialize>::deserialize(
            deserializer,
        )
        .map(Into::into)
    }
}

#[pymethods]
impl NativeExtension {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (schema, namespace, capabilities, payload, extra))]
    #[new]
    pub fn new(
        schema: String,
        namespace: String,
        capabilities: Vec<String>,
        payload: String,
        extra: HashMap<String, String>,
    ) -> Self {
        Self { schema, namespace, capabilities, payload, extra }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::parsed::NativeExtension>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct NativeMergeResult {
    #[pyo3(get)]
    pub outcome: ThreeWayMergeOutcome,
    #[pyo3(get)]
    pub diagnostics: Vec<Diagnostic>,
    #[pyo3(get)]
    pub conflicts: Vec<MergeConflict>,
    #[pyo3(get)]
    pub output: Option<String>,
    #[pyo3(get)]
    pub policies: Vec<PolicyReference>,
    #[pyo3(get)]
    pub rejected_parse: Option<CoreParseResult>,
    #[pyo3(get)]
    pub sources: Vec<SourceDescriptor>,
    #[pyo3(get)]
    pub output_source: Option<SourceDescriptor>,
    #[pyo3(get)]
    pub source_segments: Vec<RetainedSourceSegment>,
}

#[pymethods]
impl NativeMergeResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (outcome, diagnostics, conflicts, policies, sources, source_segments, output=None, rejected_parse=None, output_source=None))]
    #[new]
    pub fn new(
        outcome: ThreeWayMergeOutcome,
        diagnostics: Vec<Diagnostic>,
        conflicts: Vec<MergeConflict>,
        policies: Vec<PolicyReference>,
        sources: Vec<SourceDescriptor>,
        source_segments: Vec<RetainedSourceSegment>,
        output: Option<String>,
        rejected_parse: Option<CoreParseResult>,
        output_source: Option<SourceDescriptor>,
    ) -> Self {
        Self {
            outcome,
            diagnostics,
            conflicts,
            output,
            policies,
            rejected_parse,
            sources,
            output_source,
            source_segments,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::NativeMergeResult>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct OwnedSourceRegion {
    #[pyo3(get)]
    pub node_id: String,
    #[pyo3(get)]
    pub region_kind: String,
    #[pyo3(get)]
    pub start_byte: usize,
    #[pyo3(get)]
    pub end_byte: usize,
    #[pyo3(get)]
    pub start_line: usize,
    #[pyo3(get)]
    pub end_line: usize,
}

#[pymethods]
impl OwnedSourceRegion {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (node_id, region_kind, start_byte, end_byte, start_line, end_line))]
    #[new]
    pub fn new(
        node_id: String,
        region_kind: String,
        start_byte: usize,
        end_byte: usize,
        start_line: usize,
        end_line: usize,
    ) -> Self {
        Self { node_id, region_kind, start_byte, end_byte, start_line, end_line }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::OwnedSourceRegion>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParseBatchRequest {
    #[pyo3(get)]
    pub items: Vec<ParseRequest>,
}

#[pymethods]
impl ParseBatchRequest {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (items))]
    #[new]
    pub fn new(items: Vec<ParseRequest>) -> Self {
        Self { items }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::ParseBatchRequest>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParseBatchResult {
    #[pyo3(get)]
    pub items: Vec<ParseOutput>,
}

#[pymethods]
impl ParseBatchResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (items))]
    #[new]
    pub fn new(items: Vec<ParseOutput>) -> Self {
        Self { items }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::ParseBatchResult>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParseComment {
    #[pyo3(get)]
    pub node_id: String,
    #[pyo3(get)]
    pub native_kind: String,
    #[pyo3(get)]
    pub attachment_hint: AttachmentHint,
    #[pyo3(get)]
    pub metadata: HashMap<String, String>,
    #[pyo3(get)]
    pub extra: HashMap<String, String>,
}
impl<'de> serde::Deserialize<'de> for ParseComment {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <structuredmerge_core::parsed::ParseComment as serde::Deserialize>::deserialize(
            deserializer,
        )
        .map(Into::into)
    }
}

#[pymethods]
impl ParseComment {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (node_id, native_kind, attachment_hint, metadata, extra))]
    #[new]
    pub fn new(
        node_id: String,
        native_kind: String,
        attachment_hint: AttachmentHint,
        metadata: HashMap<String, String>,
        extra: HashMap<String, String>,
    ) -> Self {
        Self { node_id, native_kind, attachment_hint, metadata, extra }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::parsed::ParseComment>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParseDiagnostic {
    #[pyo3(get)]
    pub id: String,
    #[pyo3(get)]
    pub severity: ParseSeverity,
    #[pyo3(get)]
    pub category: String,
    #[pyo3(get)]
    pub code: Option<String>,
    #[pyo3(get)]
    pub message: String,
    #[pyo3(get)]
    pub source_role: SourceRole,
    #[pyo3(get)]
    pub span: Option<SourceSpan>,
    #[pyo3(get)]
    pub node_id: Option<String>,
    #[pyo3(get)]
    pub blocking: bool,
    #[pyo3(get)]
    pub metadata: HashMap<String, String>,
    #[pyo3(get)]
    pub extra: HashMap<String, String>,
}
impl<'de> serde::Deserialize<'de> for ParseDiagnostic {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <structuredmerge_core::parsed::ParseDiagnostic as serde::Deserialize>::deserialize(
            deserializer,
        )
        .map(Into::into)
    }
}

#[pymethods]
impl ParseDiagnostic {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (id, severity, category, message, source_role, blocking, metadata, extra, code=None, span=None, node_id=None))]
    #[new]
    pub fn new(
        id: String,
        severity: ParseSeverity,
        category: String,
        message: String,
        source_role: SourceRole,
        blocking: bool,
        metadata: HashMap<String, String>,
        extra: HashMap<String, String>,
        code: Option<String>,
        span: Option<SourceSpan>,
        node_id: Option<String>,
    ) -> Self {
        Self {
            id,
            severity,
            category,
            code,
            message,
            source_role,
            span,
            node_id,
            blocking,
            metadata,
            extra,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::parsed::ParseDiagnostic>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParseLimits {
    #[pyo3(get)]
    pub max_batch_items: usize,
    #[pyo3(get)]
    pub max_input_bytes: u64,
    #[pyo3(get)]
    pub max_nodes: usize,
    #[pyo3(get)]
    pub max_diagnostics: usize,
}

#[pymethods]
impl ParseLimits {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (max_batch_items, max_input_bytes, max_nodes, max_diagnostics))]
    #[new]
    pub fn new(
        max_batch_items: usize,
        max_input_bytes: u64,
        max_nodes: usize,
        max_diagnostics: usize,
    ) -> Self {
        Self { max_batch_items, max_input_bytes, max_nodes, max_diagnostics }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::ParseLimits>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParseNode {
    #[pyo3(get)]
    pub id: String,
    #[pyo3(get)]
    #[serde(rename = "type")]
    pub kind: String,
    #[pyo3(get)]
    pub native_type: String,
    #[pyo3(get)]
    pub role: NodeRole,
    #[pyo3(get)]
    pub named: bool,
    #[pyo3(get)]
    pub missing: bool,
    #[pyo3(get)]
    pub has_error: bool,
    #[pyo3(get)]
    pub span: SourceSpan,
    #[pyo3(get)]
    pub parent_id: Option<String>,
    #[pyo3(get)]
    pub children: Vec<ChildEdge>,
    #[pyo3(get)]
    pub semantic_roles: Vec<String>,
    #[pyo3(get)]
    pub unsupported_features: Vec<String>,
    #[pyo3(get)]
    pub extensions: Vec<NativeExtension>,
    #[pyo3(get)]
    pub metadata: HashMap<String, String>,
    #[pyo3(get)]
    pub extra: HashMap<String, String>,
}
impl<'de> serde::Deserialize<'de> for ParseNode {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <structuredmerge_core::parsed::ParseNode as serde::Deserialize>::deserialize(deserializer)
            .map(Into::into)
    }
}

#[pymethods]
impl ParseNode {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (id, r#type, native_type, role, named, missing, has_error, span, children, semantic_roles, unsupported_features, extensions, metadata, extra, parent_id=None))]
    #[new]
    pub fn new(
        id: String,
        r#type: String,
        native_type: String,
        role: NodeRole,
        named: bool,
        missing: bool,
        has_error: bool,
        span: SourceSpan,
        children: Vec<ChildEdge>,
        semantic_roles: Vec<String>,
        unsupported_features: Vec<String>,
        extensions: Vec<NativeExtension>,
        metadata: HashMap<String, String>,
        extra: HashMap<String, String>,
        parent_id: Option<String>,
    ) -> Self {
        Self {
            id,
            kind: r#type,
            native_type,
            role,
            named,
            missing,
            has_error,
            span,
            parent_id,
            children,
            semantic_roles,
            unsupported_features,
            extensions,
            metadata,
            extra,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::parsed::ParseNode>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParseOptions {
    #[pyo3(get)]
    pub comments: bool,
    #[pyo3(get)]
    pub tokens: bool,
    #[pyo3(get)]
    pub diagnostics: bool,
    #[pyo3(get)]
    pub native_extensions: bool,
}

impl Default for ParseOptions {
    fn default() -> Self {
        <structuredmerge_core::service::ParseOptions as Default>::default().into()
    }
}

#[pymethods]
impl ParseOptions {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (comments=Self::default().comments, tokens=Self::default().tokens, diagnostics=Self::default().diagnostics, native_extensions=Self::default().native_extensions))]
    #[new]
    pub fn new(comments: bool, tokens: bool, diagnostics: bool, native_extensions: bool) -> Self {
        Self { comments, tokens, diagnostics, native_extensions }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::service::ParseOptions>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParseOutput {
    #[pyo3(get)]
    pub request_id: String,
    #[pyo3(get)]
    pub source: SourceDescriptor,
    #[pyo3(get)]
    pub ok: bool,
    #[pyo3(get)]
    pub root_id: Option<String>,
    #[pyo3(get)]
    pub nodes: Vec<ParseNode>,
    #[pyo3(get)]
    pub comments: Vec<ParseComment>,
    #[pyo3(get)]
    pub diagnostics: Vec<ParseDiagnostic>,
    #[pyo3(get)]
    pub extensions: Vec<NativeExtension>,
    #[pyo3(get)]
    pub metadata: HashMap<String, String>,
    #[pyo3(get)]
    pub extra: HashMap<String, String>,
}
impl<'de> serde::Deserialize<'de> for ParseOutput {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <structuredmerge_core::parsed::ParseOutput as serde::Deserialize>::deserialize(deserializer)
            .map(Into::into)
    }
}

#[pymethods]
impl ParseOutput {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (request_id, source, ok, nodes, comments, diagnostics, extensions, metadata, extra, root_id=None))]
    #[new]
    pub fn new(
        request_id: String,
        source: SourceDescriptor,
        ok: bool,
        nodes: Vec<ParseNode>,
        comments: Vec<ParseComment>,
        diagnostics: Vec<ParseDiagnostic>,
        extensions: Vec<NativeExtension>,
        metadata: HashMap<String, String>,
        extra: HashMap<String, String>,
        root_id: Option<String>,
    ) -> Self {
        Self {
            request_id,
            source,
            ok,
            root_id,
            nodes,
            comments,
            diagnostics,
            extensions,
            metadata,
            extra,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::parsed::ParseOutput>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParseRequest {
    #[pyo3(get)]
    pub schema: String,
    #[pyo3(get)]
    pub request_id: String,
    #[pyo3(get)]
    pub source: SourceInput,
    #[pyo3(get)]
    pub language: String,
    #[pyo3(get)]
    pub dialect: Option<String>,
    #[pyo3(get)]
    pub selection: ParserSelection,
    #[pyo3(get)]
    pub options: ParseOptions,
    #[pyo3(get)]
    pub metadata: HashMap<String, String>,
    #[pyo3(get)]
    pub extra: HashMap<String, String>,
}
impl<'de> serde::Deserialize<'de> for ParseRequest {
    fn deserialize<D>(deserializer: D) -> ::core::result::Result<Self, D::Error>
    where
        D: serde::Deserializer<'de>,
    {
        <structuredmerge_core::service::ParseRequest as serde::Deserialize>::deserialize(
            deserializer,
        )
        .map(Into::into)
    }
}

#[pymethods]
impl ParseRequest {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (schema, request_id, source, language, selection, options, metadata, extra, dialect=None))]
    #[new]
    pub fn new(
        schema: String,
        request_id: String,
        source: SourceInput,
        language: String,
        selection: ParserSelection,
        options: ParseOptions,
        metadata: HashMap<String, String>,
        extra: HashMap<String, String>,
        dialect: Option<String>,
    ) -> Self {
        Self { schema, request_id, source, language, dialect, selection, options, metadata, extra }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::service::ParseRequest>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParserCandidate {
    #[pyo3(get)]
    pub backend_id: String,
    #[pyo3(get)]
    pub rejections: Vec<String>,
    #[pyo3(get)]
    pub available: Option<bool>,
    #[pyo3(get)]
    pub loadable: Option<bool>,
    #[pyo3(get)]
    pub probe_fault: Option<String>,
    #[pyo3(get)]
    pub request_preference: Option<usize>,
    #[pyo3(get)]
    pub profile_preference: Option<usize>,
    #[pyo3(get)]
    pub priority: i32,
    #[pyo3(get)]
    pub selected: bool,
}

#[pymethods]
impl ParserCandidate {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (backend_id, rejections, priority, selected, available=None, loadable=None, probe_fault=None, request_preference=None, profile_preference=None))]
    #[new]
    pub fn new(
        backend_id: String,
        rejections: Vec<String>,
        priority: i32,
        selected: bool,
        available: Option<bool>,
        loadable: Option<bool>,
        probe_fault: Option<String>,
        request_preference: Option<usize>,
        profile_preference: Option<usize>,
    ) -> Self {
        Self {
            backend_id,
            rejections,
            available,
            loadable,
            probe_fault,
            request_preference,
            profile_preference,
            priority,
            selected,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::service::ParserCandidate>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParserProbeRequest {
    #[pyo3(get)]
    pub language: String,
    #[pyo3(get)]
    pub dialect: Option<String>,
}

#[pymethods]
impl ParserProbeRequest {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (language, dialect=None))]
    #[new]
    pub fn new(language: String, dialect: Option<String>) -> Self {
        Self { language, dialect }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::service::ParserProbeRequest>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParserProbeResult {
    #[pyo3(get)]
    pub available: bool,
    #[pyo3(get)]
    pub loadable: bool,
}

#[pymethods]
impl ParserProbeResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (available, loadable))]
    #[new]
    pub fn new(available: bool, loadable: bool) -> Self {
        Self { available, loadable }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::service::ParserProbeResult>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParserProviderDescriptor {
    #[pyo3(get)]
    pub id: String,
    #[pyo3(get)]
    pub family: String,
    #[pyo3(get)]
    pub runtime: String,
    #[pyo3(get)]
    pub package: String,
    #[pyo3(get)]
    pub package_version: String,
    #[pyo3(get)]
    pub parser: String,
    #[pyo3(get)]
    pub parser_version: String,
    #[pyo3(get)]
    pub grammar: Option<String>,
    #[pyo3(get)]
    pub grammar_version: Option<String>,
    #[pyo3(get)]
    pub languages: Vec<String>,
    #[pyo3(get)]
    pub dialects: Vec<String>,
    #[pyo3(get)]
    pub contracts: Vec<String>,
    #[pyo3(get)]
    pub capabilities: Vec<String>,
    #[pyo3(get)]
    pub probe_id: String,
    #[pyo3(get)]
    pub priority: i32,
    #[pyo3(get)]
    pub metadata: HashMap<String, String>,
    #[pyo3(get)]
    pub extensions: Vec<NativeExtension>,
}

#[pymethods]
impl ParserProviderDescriptor {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (id, family, runtime, package, package_version, parser, parser_version, languages, dialects, contracts, capabilities, probe_id, priority, metadata, extensions, grammar=None, grammar_version=None))]
    #[new]
    pub fn new(
        id: String,
        family: String,
        runtime: String,
        package: String,
        package_version: String,
        parser: String,
        parser_version: String,
        languages: Vec<String>,
        dialects: Vec<String>,
        contracts: Vec<String>,
        capabilities: Vec<String>,
        probe_id: String,
        priority: i32,
        metadata: HashMap<String, String>,
        extensions: Vec<NativeExtension>,
        grammar: Option<String>,
        grammar_version: Option<String>,
    ) -> Self {
        Self {
            id,
            family,
            runtime,
            package,
            package_version,
            parser,
            parser_version,
            grammar,
            grammar_version,
            languages,
            dialects,
            contracts,
            capabilities,
            probe_id,
            priority,
            metadata,
            extensions,
        }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::service::ParserProviderDescriptor>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ParserSelection {
    #[pyo3(get)]
    pub backend_id: Option<String>,
    #[pyo3(get)]
    pub preference: Vec<String>,
    #[pyo3(get)]
    pub required_capabilities: Vec<String>,
}

#[pymethods]
impl ParserSelection {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (preference, required_capabilities, backend_id=None))]
    #[new]
    pub fn new(
        preference: Vec<String>,
        required_capabilities: Vec<String>,
        backend_id: Option<String>,
    ) -> Self {
        Self { backend_id, preference, required_capabilities }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::service::ParserSelection>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct PolicyReference {
    #[pyo3(get)]
    pub surface: PolicySurface,
    #[pyo3(get)]
    pub name: String,
}

#[pymethods]
impl PolicyReference {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (surface, name))]
    #[new]
    pub fn new(surface: PolicySurface, name: String) -> Self {
        Self { surface, name }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::PolicyReference>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ProbeBatchRequest {
    #[pyo3(get)]
    pub items: Vec<ParserProbeRequest>,
}

#[pymethods]
impl ProbeBatchRequest {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (items))]
    #[new]
    pub fn new(items: Vec<ParserProbeRequest>) -> Self {
        Self { items }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::ProbeBatchRequest>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ProbeBatchResult {
    #[pyo3(get)]
    pub items: Vec<ParserProbeResult>,
}

#[pymethods]
impl ProbeBatchResult {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (items))]
    #[new]
    pub fn new(items: Vec<ParserProbeResult>) -> Self {
        Self { items }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::ProbeBatchResult>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct RetainedSourceSegment {
    #[pyo3(get)]
    pub id: String,
    #[pyo3(get)]
    pub source_id: String,
    #[pyo3(get)]
    pub source_role: SourceRole,
    #[pyo3(get)]
    pub source_range: ByteRange,
    #[pyo3(get)]
    pub output_range: ByteRange,
    #[pyo3(get)]
    pub sha256: String,
    #[pyo3(get)]
    pub owner_id: Option<String>,
}

#[pymethods]
impl RetainedSourceSegment {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (id, source_id, source_role, source_range, output_range, sha256, owner_id=None))]
    #[new]
    pub fn new(
        id: String,
        source_id: String,
        source_role: SourceRole,
        source_range: ByteRange,
        output_range: ByteRange,
        sha256: String,
        owner_id: Option<String>,
    ) -> Self {
        Self { id, source_id, source_role, source_range, output_range, sha256, owner_id }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::RetainedSourceSegment>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ReviewDiagnosticDetail {
    #[pyo3(get)]
    pub request_id: Option<String>,
    #[pyo3(get)]
    pub action: Option<ReviewDecisionAction>,
    #[pyo3(get)]
    pub reason: Option<ReviewDiagnosticReason>,
    #[pyo3(get)]
    pub payload_kind: Option<String>,
    #[pyo3(get)]
    pub expected_family: Option<String>,
    #[pyo3(get)]
    pub provided_family: Option<String>,
}

#[pymethods]
impl ReviewDiagnosticDetail {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (request_id=None, action=None, reason=None, payload_kind=None, expected_family=None, provided_family=None))]
    #[new]
    pub fn new(
        request_id: Option<String>,
        action: Option<ReviewDecisionAction>,
        reason: Option<ReviewDiagnosticReason>,
        payload_kind: Option<String>,
        expected_family: Option<String>,
        provided_family: Option<String>,
    ) -> Self {
        Self { request_id, action, reason, payload_kind, expected_family, provided_family }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::ReviewDiagnosticDetail>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SelectionReport {
    #[pyo3(get)]
    pub requested: ParserSelection,
    #[pyo3(get)]
    pub generation: u64,
    #[pyo3(get)]
    pub digest: String,
    #[pyo3(get)]
    pub candidates: Vec<ParserCandidate>,
    #[pyo3(get)]
    pub selected_backend: Option<String>,
}

#[pymethods]
impl SelectionReport {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (requested, generation, digest, candidates, selected_backend=None))]
    #[new]
    pub fn new(
        requested: ParserSelection,
        generation: u64,
        digest: String,
        candidates: Vec<ParserCandidate>,
        selected_backend: Option<String>,
    ) -> Self {
        Self { requested, generation, digest, candidates, selected_backend }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::service::SelectionReport>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SourceDescriptor {
    #[pyo3(get)]
    pub source_id: String,
    #[pyo3(get)]
    pub role: SourceRole,
    #[pyo3(get)]
    pub byte_length: u64,
    #[pyo3(get)]
    pub sha256: String,
    #[pyo3(get)]
    pub encoding: SourceEncoding,
    #[pyo3(get)]
    pub bom: bool,
    #[pyo3(get)]
    pub line_endings: LineEndings,
    #[pyo3(get)]
    pub final_newline: bool,
}

#[pymethods]
impl SourceDescriptor {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (source_id, role, byte_length, sha256, encoding, bom, line_endings, final_newline))]
    #[new]
    pub fn new(
        source_id: String,
        role: SourceRole,
        byte_length: u64,
        sha256: String,
        encoding: SourceEncoding,
        bom: bool,
        line_endings: LineEndings,
        final_newline: bool,
    ) -> Self {
        Self { source_id, role, byte_length, sha256, encoding, bom, line_endings, final_newline }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::source::SourceDescriptor>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SourceInput {
    #[pyo3(get)]
    pub descriptor: SourceDescriptor,
    #[pyo3(get)]
    pub bytes: Vec<u8>,
}

#[pymethods]
impl SourceInput {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (descriptor, bytes))]
    #[new]
    pub fn new(descriptor: SourceDescriptor, bytes: Vec<u8>) -> Self {
        Self { descriptor, bytes }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::source::SourceInput>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SourcePoint {
    #[pyo3(get)]
    pub row: usize,
    #[pyo3(get)]
    pub column: usize,
}

#[pymethods]
impl SourcePoint {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (row, column))]
    #[new]
    pub fn new(row: usize, column: usize) -> Self {
        Self { row, column }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::SourcePoint>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SourceSpan {
    #[pyo3(get)]
    pub range: ByteRange,
    #[pyo3(get)]
    pub start_point: SourcePoint,
    #[pyo3(get)]
    pub end_point: SourcePoint,
}

#[pymethods]
impl SourceSpan {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (range, start_point, end_point))]
    #[new]
    pub fn new(range: ByteRange, start_point: SourcePoint, end_point: SourcePoint) -> Self {
        Self { range, start_point, end_point }
    }

    #[staticmethod]
    fn from_json(json_str: String) -> pyo3::PyResult<Self> {
        serde_json::from_str::<structuredmerge_core::SourceSpan>(&json_str)
            .map(Into::into)
            .map_err(|error| pyo3::exceptions::PyValueError::new_err(error.to_string()))
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum AttachmentHint {
    #[pyo3(name = "LEADING")]
    #[default]
    Leading = 0,
    #[pyo3(name = "INLINE")]
    Inline = 1,
    #[pyo3(name = "TRAILING")]
    Trailing = 2,
    #[pyo3(name = "PREAMBLE")]
    Preamble = 3,
    #[pyo3(name = "POSTLUDE")]
    Postlude = 4,
    #[pyo3(name = "ORPHAN")]
    Orphan = 5,
    #[pyo3(name = "UNKNOWN")]
    Unknown = 6,
}
#[pymethods]
impl AttachmentHint {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "leading" => return Ok(Self::Leading),
                "inline" => return Ok(Self::Inline),
                "trailing" => return Ok(Self::Trailing),
                "preamble" => return Ok(Self::Preamble),
                "postlude" => return Ok(Self::Postlude),
                "orphan" => return Ok(Self::Orphan),
                "unknown" => return Ok(Self::Unknown),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Leading),
                1 => return Ok(Self::Inline),
                2 => return Ok(Self::Trailing),
                3 => return Ok(Self::Preamble),
                4 => return Ok(Self::Postlude),
                5 => return Ok(Self::Orphan),
                6 => return Ok(Self::Unknown),
                _ => {}
            }
        }
        let type_name = stringify!(AttachmentHint);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize AttachmentHint: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ConflictAlternativeState {
    #[pyo3(name = "PRESENT")]
    #[default]
    Present = 0,
    #[pyo3(name = "ABSENT")]
    Absent = 1,
}
#[pymethods]
impl ConflictAlternativeState {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "present" => return Ok(Self::Present),
                "absent" => return Ok(Self::Absent),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Present),
                1 => return Ok(Self::Absent),
                _ => {}
            }
        }
        let type_name = stringify!(ConflictAlternativeState);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize ConflictAlternativeState: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum DiagnosticCategory {
    #[pyo3(name = "PARSE_ERROR")]
    #[default]
    ParseError = 0,
    #[pyo3(name = "DESTINATION_PARSE_ERROR")]
    DestinationParseError = 1,
    #[pyo3(name = "UNSUPPORTED_FEATURE")]
    UnsupportedFeature = 2,
    #[pyo3(name = "FALLBACK_APPLIED")]
    FallbackApplied = 3,
    #[pyo3(name = "AMBIGUITY")]
    Ambiguity = 4,
    #[pyo3(name = "MERGE_CONFLICT")]
    MergeConflict = 5,
    #[pyo3(name = "KIND_MISMATCH")]
    KindMismatch = 6,
    #[pyo3(name = "UNSUPPORTED_VERSION")]
    UnsupportedVersion = 7,
    #[pyo3(name = "ASSUMED_DEFAULT")]
    AssumedDefault = 8,
    #[pyo3(name = "CONFIGURATION_ERROR")]
    ConfigurationError = 9,
    #[pyo3(name = "REPLAY_REJECTED")]
    ReplayRejected = 10,
}
#[pymethods]
impl DiagnosticCategory {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "parseerror" => return Ok(Self::ParseError),
                "parse_error" => return Ok(Self::ParseError),
                "destinationparseerror" => return Ok(Self::DestinationParseError),
                "destination_parse_error" => return Ok(Self::DestinationParseError),
                "unsupportedfeature" => return Ok(Self::UnsupportedFeature),
                "unsupported_feature" => return Ok(Self::UnsupportedFeature),
                "fallbackapplied" => return Ok(Self::FallbackApplied),
                "fallback_applied" => return Ok(Self::FallbackApplied),
                "ambiguity" => return Ok(Self::Ambiguity),
                "mergeconflict" => return Ok(Self::MergeConflict),
                "merge_conflict" => return Ok(Self::MergeConflict),
                "kindmismatch" => return Ok(Self::KindMismatch),
                "kind_mismatch" => return Ok(Self::KindMismatch),
                "unsupportedversion" => return Ok(Self::UnsupportedVersion),
                "unsupported_version" => return Ok(Self::UnsupportedVersion),
                "assumeddefault" => return Ok(Self::AssumedDefault),
                "assumed_default" => return Ok(Self::AssumedDefault),
                "configurationerror" => return Ok(Self::ConfigurationError),
                "configuration_error" => return Ok(Self::ConfigurationError),
                "replayrejected" => return Ok(Self::ReplayRejected),
                "replay_rejected" => return Ok(Self::ReplayRejected),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::ParseError),
                1 => return Ok(Self::DestinationParseError),
                2 => return Ok(Self::UnsupportedFeature),
                3 => return Ok(Self::FallbackApplied),
                4 => return Ok(Self::Ambiguity),
                5 => return Ok(Self::MergeConflict),
                6 => return Ok(Self::KindMismatch),
                7 => return Ok(Self::UnsupportedVersion),
                8 => return Ok(Self::AssumedDefault),
                9 => return Ok(Self::ConfigurationError),
                10 => return Ok(Self::ReplayRejected),
                _ => {}
            }
        }
        let type_name = stringify!(DiagnosticCategory);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize DiagnosticCategory: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum DiagnosticSeverity {
    #[pyo3(name = "INFO")]
    #[default]
    Info = 0,
    #[pyo3(name = "WARNING")]
    Warning = 1,
    #[pyo3(name = "ERROR")]
    Error = 2,
}
#[pymethods]
impl DiagnosticSeverity {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "info" => return Ok(Self::Info),
                "warning" => return Ok(Self::Warning),
                "error" => return Ok(Self::Error),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Info),
                1 => return Ok(Self::Warning),
                2 => return Ok(Self::Error),
                _ => {}
            }
        }
        let type_name = stringify!(DiagnosticSeverity);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize DiagnosticSeverity: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum NodeRole {
    #[pyo3(name = "STRUCTURAL")]
    #[default]
    Structural = 0,
    #[pyo3(name = "TOKEN")]
    Token = 1,
    #[pyo3(name = "TRIVIA")]
    Trivia = 2,
    #[pyo3(name = "COMMENT")]
    Comment = 3,
    #[pyo3(name = "DELIMITER")]
    Delimiter = 4,
    #[pyo3(name = "SEPARATOR")]
    Separator = 5,
    #[pyo3(name = "VIRTUAL")]
    Virtual = 6,
    #[pyo3(name = "ERROR")]
    Error = 7,
    #[pyo3(name = "OPAQUE")]
    Opaque = 8,
}
#[pymethods]
impl NodeRole {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "structural" => return Ok(Self::Structural),
                "token" => return Ok(Self::Token),
                "trivia" => return Ok(Self::Trivia),
                "comment" => return Ok(Self::Comment),
                "delimiter" => return Ok(Self::Delimiter),
                "separator" => return Ok(Self::Separator),
                "virtual" => return Ok(Self::Virtual),
                "error" => return Ok(Self::Error),
                "opaque" => return Ok(Self::Opaque),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Structural),
                1 => return Ok(Self::Token),
                2 => return Ok(Self::Trivia),
                3 => return Ok(Self::Comment),
                4 => return Ok(Self::Delimiter),
                5 => return Ok(Self::Separator),
                6 => return Ok(Self::Virtual),
                7 => return Ok(Self::Error),
                8 => return Ok(Self::Opaque),
                _ => {}
            }
        }
        let type_name = stringify!(NodeRole);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize NodeRole: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ParseSeverity {
    #[pyo3(name = "INFO")]
    #[default]
    Info = 0,
    #[pyo3(name = "WARNING")]
    Warning = 1,
    #[pyo3(name = "ERROR")]
    Error = 2,
}
#[pymethods]
impl ParseSeverity {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "info" => return Ok(Self::Info),
                "warning" => return Ok(Self::Warning),
                "error" => return Ok(Self::Error),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Info),
                1 => return Ok(Self::Warning),
                2 => return Ok(Self::Error),
                _ => {}
            }
        }
        let type_name = stringify!(ParseSeverity);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize ParseSeverity: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum PolicySurface {
    #[pyo3(name = "FALLBACK")]
    #[default]
    Fallback = 0,
    #[pyo3(name = "ARRAY")]
    Array = 1,
}
#[pymethods]
impl PolicySurface {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "fallback" => return Ok(Self::Fallback),
                "array" => return Ok(Self::Array),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Fallback),
                1 => return Ok(Self::Array),
                _ => {}
            }
        }
        let type_name = stringify!(PolicySurface);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize PolicySurface: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ReviewDecisionAction {
    #[pyo3(name = "ACCEPT_DEFAULT_CONTEXT")]
    #[default]
    AcceptDefaultContext = 0,
    #[pyo3(name = "PROVIDE_EXPLICIT_CONTEXT")]
    ProvideExplicitContext = 1,
    #[pyo3(name = "APPLY_DELEGATED_CHILD_GROUP")]
    ApplyDelegatedChildGroup = 2,
}
#[pymethods]
impl ReviewDecisionAction {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "acceptdefaultcontext" => return Ok(Self::AcceptDefaultContext),
                "accept_default_context" => return Ok(Self::AcceptDefaultContext),
                "provideexplicitcontext" => return Ok(Self::ProvideExplicitContext),
                "provide_explicit_context" => return Ok(Self::ProvideExplicitContext),
                "applydelegatedchildgroup" => return Ok(Self::ApplyDelegatedChildGroup),
                "apply_delegated_child_group" => return Ok(Self::ApplyDelegatedChildGroup),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::AcceptDefaultContext),
                1 => return Ok(Self::ProvideExplicitContext),
                2 => return Ok(Self::ApplyDelegatedChildGroup),
                _ => {}
            }
        }
        let type_name = stringify!(ReviewDecisionAction);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize ReviewDecisionAction: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ReviewDiagnosticReason {
    #[pyo3(name = "MISSING_REQUIRED_PAYLOAD")]
    #[default]
    MissingRequiredPayload = 0,
    #[pyo3(name = "FAMILY_MISMATCH")]
    FamilyMismatch = 1,
    #[pyo3(name = "REQUEST_NOT_FOUND")]
    RequestNotFound = 2,
}
#[pymethods]
impl ReviewDiagnosticReason {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "missingrequiredpayload" => return Ok(Self::MissingRequiredPayload),
                "missing_required_payload" => return Ok(Self::MissingRequiredPayload),
                "familymismatch" => return Ok(Self::FamilyMismatch),
                "family_mismatch" => return Ok(Self::FamilyMismatch),
                "requestnotfound" => return Ok(Self::RequestNotFound),
                "request_not_found" => return Ok(Self::RequestNotFound),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::MissingRequiredPayload),
                1 => return Ok(Self::FamilyMismatch),
                2 => return Ok(Self::RequestNotFound),
                _ => {}
            }
        }
        let type_name = stringify!(ReviewDiagnosticReason);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize ReviewDiagnosticReason: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum SourceEncoding {
    #[pyo3(name = "UTF8")]
    #[default]
    Utf8 = 0,
    #[pyo3(name = "BINARY")]
    Binary = 1,
}
#[pymethods]
impl SourceEncoding {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "utf8" => return Ok(Self::Utf8),
                "binary" => return Ok(Self::Binary),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Utf8),
                1 => return Ok(Self::Binary),
                _ => {}
            }
        }
        let type_name = stringify!(SourceEncoding);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize SourceEncoding: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum SourceRevision {
    #[pyo3(name = "BASE")]
    #[default]
    Base = 0,
    #[pyo3(name = "OURS")]
    Ours = 1,
    #[pyo3(name = "THEIRS")]
    Theirs = 2,
}
#[pymethods]
impl SourceRevision {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "base" => return Ok(Self::Base),
                "ours" => return Ok(Self::Ours),
                "theirs" => return Ok(Self::Theirs),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Base),
                1 => return Ok(Self::Ours),
                2 => return Ok(Self::Theirs),
                _ => {}
            }
        }
        let type_name = stringify!(SourceRevision);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize SourceRevision: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum SourceRole {
    #[pyo3(name = "SOURCE")]
    #[default]
    Source = 0,
    #[pyo3(name = "BEFORE")]
    Before = 1,
    #[pyo3(name = "AFTER")]
    After = 2,
    #[pyo3(name = "INCOMING")]
    Incoming = 3,
    #[pyo3(name = "CURRENT")]
    Current = 4,
    #[pyo3(name = "BASE")]
    Base = 5,
    #[pyo3(name = "OURS")]
    Ours = 6,
    #[pyo3(name = "THEIRS")]
    Theirs = 7,
    #[pyo3(name = "OUTPUT")]
    Output = 8,
}
#[pymethods]
impl SourceRole {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "source" => return Ok(Self::Source),
                "before" => return Ok(Self::Before),
                "after" => return Ok(Self::After),
                "incoming" => return Ok(Self::Incoming),
                "current" => return Ok(Self::Current),
                "base" => return Ok(Self::Base),
                "ours" => return Ok(Self::Ours),
                "theirs" => return Ok(Self::Theirs),
                "output" => return Ok(Self::Output),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Source),
                1 => return Ok(Self::Before),
                2 => return Ok(Self::After),
                3 => return Ok(Self::Incoming),
                4 => return Ok(Self::Current),
                5 => return Ok(Self::Base),
                6 => return Ok(Self::Ours),
                7 => return Ok(Self::Theirs),
                8 => return Ok(Self::Output),
                _ => {}
            }
        }
        let type_name = stringify!(SourceRole);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize SourceRole: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "snake_case")]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ThreeWayMergeOutcome {
    #[pyo3(name = "CLEAN")]
    #[default]
    Clean = 0,
    #[pyo3(name = "CONFLICT")]
    Conflict = 1,
    #[pyo3(name = "ERROR")]
    Error = 2,
}
#[pymethods]
impl ThreeWayMergeOutcome {
    #[new]
    fn __new__(value: pyo3::Bound<'_, pyo3::PyAny>) -> pyo3::PyResult<Self> {
        use pyo3::prelude::*;
        // Try to extract as string first
        if let Ok(s) = value.extract::<&str>() {
            let s_lower = s.to_lowercase();
            match s_lower.as_str() {
                "clean" => return Ok(Self::Clean),
                "conflict" => return Ok(Self::Conflict),
                "error" => return Ok(Self::Error),
                _ => {}
            }
        }
        // Try to extract as integer (by discriminant value)
        if let Ok(n) = value.extract::<i32>() {
            match n {
                0 => return Ok(Self::Clean),
                1 => return Ok(Self::Conflict),
                2 => return Ok(Self::Error),
                _ => {}
            }
        }
        let type_name = stringify!(ThreeWayMergeOutcome);
        Err(pyo3::exceptions::PyValueError::new_err(format!(
            "invalid value for {}: {:#?}. Expected variant name (str) or discriminant (int)",
            type_name, value
        )))
    }

    fn __str__(&self) -> PyResult<String> {
        serde_json::to_value(self)
            .map(|value| match value {
                serde_json::Value::String(value) => value,
                other => other.to_string(),
            })
            .map_err(|e| {
                pyo3::exceptions::PyRuntimeError::new_err(format!(
                    "Failed to serialize ThreeWayMergeOutcome: {e}"
                ))
            })
    }

    fn __repr__(&self) -> PyResult<String> {
        self.__str__()
    }
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (requests, limits))]
pub fn merge_python_declarations(
    py: Python<'_>,
    requests: Vec<ParseRequest>,
    limits: ParseLimits,
) -> PyResult<NativeMergeResult> {
    let requests_core: Vec<_> = requests.into_iter().map(Into::into).collect();

    let limits_core: structuredmerge_core::ParseLimits = limits.into();

    py.detach(|| structuredmerge_core::merge_python_declarations(requests_core, limits_core))
        .map(Into::into)
        .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (requests, limits))]
pub fn merge_yaml_mapping(
    py: Python<'_>,
    requests: Vec<ParseRequest>,
    limits: ParseLimits,
) -> PyResult<NativeMergeResult> {
    let requests_core: Vec<_> = requests.into_iter().map(Into::into).collect();

    let limits_core: structuredmerge_core::ParseLimits = limits.into();

    py.detach(|| structuredmerge_core::merge_yaml_mapping(requests_core, limits_core))
        .map(Into::into)
        .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (requests, limits))]
pub fn parse_sources(
    py: Python<'_>,
    requests: Vec<ParseRequest>,
    limits: ParseLimits,
) -> PyResult<Vec<CoreParseResult>> {
    let requests_core: Vec<_> = requests.into_iter().map(Into::into).collect();

    let limits_core: structuredmerge_core::ParseLimits = limits.into();

    py.detach(|| structuredmerge_core::parse_sources(requests_core, limits_core))
        .map(|val| val.into_iter().map(Into::into).collect())
        .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (host))]
pub fn register_parser_host(host: Py<PyAny>) -> PyResult<()> {
    let host = {
        let bridge = PyParserHostBridge::new(host)?;
        std::sync::Arc::new(bridge) as std::sync::Arc<dyn structuredmerge_core::ParserHost>
    };
    structuredmerge_core::register_parser_host(host)
        .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (id))]
pub fn unregister_parser_host(py: Python<'_>, id: String) -> PyResult<()> {
    py.detach(|| structuredmerge_core::unregister_parser_host(id))
        .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))
}

#[pyclass(name = "ParserHost")]
pub struct PyParserHostMarker;

/// Lift a native binding object into the package's public options dataclass via the
/// generated `options._from_native_*` converter, so trait-callback hosts receive the
/// type the package exports under that name. Falls back to the native object when the
/// options module is unavailable (attribute access is identical either way).
#[allow(dead_code)]
fn __alef_options_from_native<'py, T>(
    py: Python<'py>,
    func: &str,
    native: T,
) -> pyo3::Py<pyo3::PyAny>
where
    T: pyo3::IntoPyObjectExt<'py>,
{
    let Ok(native) = pyo3::IntoPyObjectExt::into_py_any(native, py) else {
        return py.None();
    };
    py.import("_native.options")
        .and_then(|m| m.getattr(func))
        .and_then(|f| f.call1((native.clone_ref(py),)))
        .map(|v| v.unbind())
        .unwrap_or(native)
}

/// Wrapper that bridges a foreign Py object to the `ParserHost` trait.
pub struct PyParserHostBridge {
    inner: Py<PyAny>,
    cached_name: String,
}

impl std::fmt::Debug for PyParserHostBridge {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(f, "PyParserHostBridge")
    }
}

impl PyParserHostBridge {
    /// Create a new bridge wrapping a Python object.
    ///
    /// Validates that the Python object provides all required methods.
    pub fn new(python_obj: Py<PyAny>) -> PyResult<Self> {
        Python::attach(|py| {
            let obj = python_obj.bind(py);
            if !obj.hasattr("descriptor").unwrap_or(false) {
                return Err(pyo3::exceptions::PyAttributeError::new_err(
                    "Python object missing required method: descriptor",
                ));
            }
            if !obj.hasattr("probe_batch").unwrap_or(false) {
                return Err(pyo3::exceptions::PyAttributeError::new_err(
                    "Python object missing required method: probe_batch",
                ));
            }
            if !obj.hasattr("parse_batch").unwrap_or(false) {
                return Err(pyo3::exceptions::PyAttributeError::new_err(
                    "Python object missing required method: parse_batch",
                ));
            }
            let cached_name: String = obj
                .call_method0("name")
                .and_then(|v| v.extract())
                .unwrap_or_else(|_| "unknown".to_string());

            Ok(Self { inner: python_obj, cached_name })
        })
    }
}

impl structuredmerge_core::ParserHost for PyParserHostBridge {
    fn descriptor(
        &self,
    ) -> std::result::Result<
        structuredmerge_core::service::ParserProviderDescriptor,
        structuredmerge_core::CoreError,
    > {
        Python::attach(|py| {
            let obj = self.inner.bind(py);
            py.import("contextvars")
                .and_then(|m| m.call_method0("copy_context"))
                .and_then(|ctx| {
                    let bound_method = obj.getattr("descriptor")?;
                    ctx.call_method1("run", (bound_method,))
                })
                .and_then(|v| {
                    // Native fast-path: the host returned the binding's result object — extract it and
                    // convert via `From<ParserProviderDescriptor>`, skipping the JSON round-trip. Fall back
                    // to the mapping path (a JSON string) when the host returned a plain mapping instead.
                    if let Ok(native) = v.extract::<ParserProviderDescriptor>() {
                        return Ok(<structuredmerge_core::service::ParserProviderDescriptor>::from(native));
                    }
                    let s = v.extract::<String>()?;
                    serde_json::from_str::<structuredmerge_core::service::ParserProviderDescriptor>(&s).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("method 'descriptor' returned a value that does not match the expected return type `ParserProviderDescriptor`: {}. The returned value must be a mapping matching the fields of `ParserProviderDescriptor`.", e)))
                })
                .map_err(|e| structuredmerge_core::CoreError::new(format!("Plugin '{}' method 'descriptor' failed: {}", self.cached_name, e)))
        })
    }

    fn probe_batch(
        &self,
        request: structuredmerge_core::ProbeBatchRequest,
    ) -> std::result::Result<structuredmerge_core::ProbeBatchResult, structuredmerge_core::CoreError>
    {
        Python::attach(|py| {
            let obj = self.inner.bind(py);
            py.import("contextvars")
                .and_then(|m| m.call_method0("copy_context"))
                .and_then(|ctx| {
                    let bound_method = obj.getattr("probe_batch")?;
                    ctx.call_method1("run", (bound_method, ProbeBatchRequest::from(request.clone()),))
                })
                .and_then(|v| {
                    // Native fast-path: the host returned the binding's result object — extract it and
                    // convert via `From<ProbeBatchResult>`, skipping the JSON round-trip. Fall back
                    // to the mapping path (a JSON string) when the host returned a plain mapping instead.
                    if let Ok(native) = v.extract::<ProbeBatchResult>() {
                        return Ok(<structuredmerge_core::ProbeBatchResult>::from(native));
                    }
                    let s = v.extract::<String>()?;
                    serde_json::from_str::<structuredmerge_core::ProbeBatchResult>(&s).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("method 'probe_batch' returned a value that does not match the expected return type `ProbeBatchResult`: {}. The returned value must be a mapping matching the fields of `ProbeBatchResult`.", e)))
                })
                .map_err(|e| structuredmerge_core::CoreError::new(format!("Plugin '{}' method 'probe_batch' failed: {}", self.cached_name, e)))
        })
    }

    fn parse_batch(
        &self,
        request: structuredmerge_core::ParseBatchRequest,
    ) -> std::result::Result<structuredmerge_core::ParseBatchResult, structuredmerge_core::CoreError>
    {
        Python::attach(|py| {
            let obj = self.inner.bind(py);
            py.import("contextvars")
                .and_then(|m| m.call_method0("copy_context"))
                .and_then(|ctx| {
                    let bound_method = obj.getattr("parse_batch")?;
                    ctx.call_method1("run", (bound_method, ParseBatchRequest::from(request.clone()),))
                })
                .and_then(|v| {
                    // Native fast-path: the host returned the binding's result object — extract it and
                    // convert via `From<ParseBatchResult>`, skipping the JSON round-trip. Fall back
                    // to the mapping path (a JSON string) when the host returned a plain mapping instead.
                    if let Ok(native) = v.extract::<ParseBatchResult>() {
                        return Ok(<structuredmerge_core::ParseBatchResult>::from(native));
                    }
                    let s = v.extract::<String>()?;
                    serde_json::from_str::<structuredmerge_core::ParseBatchResult>(&s).map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("method 'parse_batch' returned a value that does not match the expected return type `ParseBatchResult`: {}. The returned value must be a mapping matching the fields of `ParseBatchResult`.", e)))
                })
                .map_err(|e| structuredmerge_core::CoreError::new(format!("Plugin '{}' method 'parse_batch' failed: {}", self.cached_name, e)))
        })
    }
}

impl From<ByteRange> for structuredmerge_core::ByteRange {
    fn from(val: ByteRange) -> Self {
        Self { start_byte: val.start_byte, end_byte: val.end_byte }
    }
}

impl From<structuredmerge_core::ByteRange> for ByteRange {
    fn from(val: structuredmerge_core::ByteRange) -> Self {
        Self { start_byte: val.start_byte, end_byte: val.end_byte }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ChildEdge> for structuredmerge_core::parsed::ChildEdge {
    fn from(val: ChildEdge) -> Self {
        Self {
            node_id: val.node_id,
            index: val.index,
            field_name: val.field_name,
            extra: val
                .extra
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<structuredmerge_core::parsed::ChildEdge> for ChildEdge {
    fn from(val: structuredmerge_core::parsed::ChildEdge) -> Self {
        Self {
            node_id: val.node_id.to_string(),
            index: val.index,
            field_name: val.field_name.map(|v| v.to_string()),
            extra: val.extra.into_iter().map(|(k, v)| (k.to_string(), v.to_string())).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ConflictAlternative> for structuredmerge_core::ConflictAlternative {
    fn from(val: ConflictAlternative) -> Self {
        Self {
            revision: val.revision.into(),
            state: val.state.into(),
            regions: val.regions.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::ConflictAlternative> for ConflictAlternative {
    fn from(val: structuredmerge_core::ConflictAlternative) -> Self {
        Self {
            revision: val.revision.into(),
            state: val.state.into(),
            regions: val.regions.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<structuredmerge_core::CoreError> for CoreError {
    fn from(val: structuredmerge_core::CoreError) -> Self {
        Self { code: val.code.to_string(), message: val.message.to_string() }
    }
}

#[allow(clippy::useless_conversion)]
impl From<CoreParseResult> for structuredmerge_core::CoreParseResult {
    fn from(val: CoreParseResult) -> Self {
        Self {
            schema: val.schema,
            selection: val.selection.into(),
            backend: val.backend.into(),
            parsed: val.parsed.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::CoreParseResult> for CoreParseResult {
    fn from(val: structuredmerge_core::CoreParseResult) -> Self {
        Self {
            schema: val.schema.to_string(),
            selection: val.selection.into(),
            backend: val.backend.into(),
            parsed: val.parsed.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<Diagnostic> for structuredmerge_core::Diagnostic {
    fn from(val: Diagnostic) -> Self {
        Self {
            severity: val.severity.into(),
            category: val.category.into(),
            message: val.message,
            path: val.path,
            review: val.review.map(Into::into).map(Box::new),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::Diagnostic> for Diagnostic {
    fn from(val: structuredmerge_core::Diagnostic) -> Self {
        Self {
            severity: val.severity.into(),
            category: val.category.into(),
            message: val.message.to_string(),
            path: val.path.map(|v| v.to_string()),
            review: val.review.map(|v| (*v).into()),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<LineEndings> for structuredmerge_core::source::LineEndings {
    fn from(val: LineEndings) -> Self {
        Self { lf: val.lf, crlf: val.crlf, bare_cr: val.bare_cr, ..Default::default() }
    }
}

impl From<structuredmerge_core::source::LineEndings> for LineEndings {
    fn from(val: structuredmerge_core::source::LineEndings) -> Self {
        Self { lf: val.lf, crlf: val.crlf, bare_cr: val.bare_cr }
    }
}

#[allow(clippy::useless_conversion)]
impl From<MergeConflict> for structuredmerge_core::MergeConflict {
    fn from(val: MergeConflict) -> Self {
        Self {
            conflict_id: val.conflict_id,
            category: val.category,
            path: val.path,
            fallback_scope: val.fallback_scope,
            message: val.message,
            alternatives: val.alternatives.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::MergeConflict> for MergeConflict {
    fn from(val: structuredmerge_core::MergeConflict) -> Self {
        Self {
            conflict_id: val.conflict_id.to_string(),
            category: val.category.to_string(),
            path: val.path.to_string(),
            fallback_scope: val.fallback_scope.to_string(),
            message: val.message.to_string(),
            alternatives: val.alternatives.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<NativeExtension> for structuredmerge_core::parsed::NativeExtension {
    fn from(val: NativeExtension) -> Self {
        Self {
            schema: val.schema,
            namespace: val.namespace,
            capabilities: val.capabilities.into_iter().collect(),
            payload: serde_json::from_str(&val.payload).unwrap_or_default(),
            extra: val
                .extra
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<structuredmerge_core::parsed::NativeExtension> for NativeExtension {
    fn from(val: structuredmerge_core::parsed::NativeExtension) -> Self {
        Self {
            schema: val.schema.to_string(),
            namespace: val.namespace.to_string(),
            capabilities: val.capabilities.into_iter().collect(),
            payload: val.payload.to_string(),
            extra: val.extra.into_iter().map(|(k, v)| (k.to_string(), v.to_string())).collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<NativeMergeResult> for structuredmerge_core::NativeMergeResult {
    fn from(val: NativeMergeResult) -> Self {
        Self {
            outcome: val.outcome.into(),
            diagnostics: val.diagnostics.into_iter().map(Into::into).collect(),
            conflicts: val.conflicts.into_iter().map(Into::into).collect(),
            output: val.output,
            policies: val.policies.into_iter().map(Into::into).collect(),
            rejected_parse: val.rejected_parse.map(Into::into),
            sources: val.sources.into_iter().map(Into::into).collect(),
            output_source: val.output_source.map(Into::into),
            source_segments: val.source_segments.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::NativeMergeResult> for NativeMergeResult {
    fn from(val: structuredmerge_core::NativeMergeResult) -> Self {
        Self {
            outcome: val.outcome.into(),
            diagnostics: val.diagnostics.into_iter().map(Into::into).collect(),
            conflicts: val.conflicts.into_iter().map(Into::into).collect(),
            output: val.output.map(|v| v.to_string()),
            policies: val.policies.into_iter().map(Into::into).collect(),
            rejected_parse: val.rejected_parse.map(Into::into),
            sources: val.sources.into_iter().map(Into::into).collect(),
            output_source: val.output_source.map(Into::into),
            source_segments: val.source_segments.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<OwnedSourceRegion> for structuredmerge_core::OwnedSourceRegion {
    fn from(val: OwnedSourceRegion) -> Self {
        Self {
            node_id: val.node_id,
            region_kind: val.region_kind,
            start_byte: val.start_byte,
            end_byte: val.end_byte,
            start_line: val.start_line,
            end_line: val.end_line,
        }
    }
}

impl From<structuredmerge_core::OwnedSourceRegion> for OwnedSourceRegion {
    fn from(val: structuredmerge_core::OwnedSourceRegion) -> Self {
        Self {
            node_id: val.node_id.to_string(),
            region_kind: val.region_kind.to_string(),
            start_byte: val.start_byte,
            end_byte: val.end_byte,
            start_line: val.start_line,
            end_line: val.end_line,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ParseBatchRequest> for structuredmerge_core::ParseBatchRequest {
    fn from(val: ParseBatchRequest) -> Self {
        Self { items: val.items.into_iter().map(Into::into).collect() }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::ParseBatchRequest> for ParseBatchRequest {
    fn from(val: structuredmerge_core::ParseBatchRequest) -> Self {
        Self { items: val.items.into_iter().map(Into::into).collect() }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ParseBatchResult> for structuredmerge_core::ParseBatchResult {
    fn from(val: ParseBatchResult) -> Self {
        Self { items: val.items.into_iter().map(Into::into).collect() }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::ParseBatchResult> for ParseBatchResult {
    fn from(val: structuredmerge_core::ParseBatchResult) -> Self {
        Self { items: val.items.into_iter().map(Into::into).collect() }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ParseComment> for structuredmerge_core::parsed::ParseComment {
    fn from(val: ParseComment) -> Self {
        Self {
            node_id: val.node_id,
            native_kind: val.native_kind,
            attachment_hint: val.attachment_hint.into(),
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
            extra: val
                .extra
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::parsed::ParseComment> for ParseComment {
    fn from(val: structuredmerge_core::parsed::ParseComment) -> Self {
        Self {
            node_id: val.node_id.to_string(),
            native_kind: val.native_kind.to_string(),
            attachment_hint: val.attachment_hint.into(),
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.to_string(), v.to_string()))
                .collect(),
            extra: val.extra.into_iter().map(|(k, v)| (k.to_string(), v.to_string())).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ParseDiagnostic> for structuredmerge_core::parsed::ParseDiagnostic {
    fn from(val: ParseDiagnostic) -> Self {
        Self {
            id: val.id,
            severity: val.severity.into(),
            category: val.category,
            code: val.code,
            message: val.message,
            source_role: val.source_role.into(),
            span: val.span.map(Into::into),
            node_id: val.node_id,
            blocking: val.blocking,
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
            extra: val
                .extra
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::parsed::ParseDiagnostic> for ParseDiagnostic {
    fn from(val: structuredmerge_core::parsed::ParseDiagnostic) -> Self {
        Self {
            id: val.id.to_string(),
            severity: val.severity.into(),
            category: val.category.to_string(),
            code: val.code.map(|v| v.to_string()),
            message: val.message.to_string(),
            source_role: val.source_role.into(),
            span: val.span.map(Into::into),
            node_id: val.node_id.map(|v| v.to_string()),
            blocking: val.blocking,
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.to_string(), v.to_string()))
                .collect(),
            extra: val.extra.into_iter().map(|(k, v)| (k.to_string(), v.to_string())).collect(),
        }
    }
}

impl From<ParseLimits> for structuredmerge_core::ParseLimits {
    fn from(val: ParseLimits) -> Self {
        Self {
            max_batch_items: val.max_batch_items,
            max_input_bytes: val.max_input_bytes,
            max_nodes: val.max_nodes,
            max_diagnostics: val.max_diagnostics,
        }
    }
}

impl From<structuredmerge_core::ParseLimits> for ParseLimits {
    fn from(val: structuredmerge_core::ParseLimits) -> Self {
        Self {
            max_batch_items: val.max_batch_items,
            max_input_bytes: val.max_input_bytes,
            max_nodes: val.max_nodes,
            max_diagnostics: val.max_diagnostics,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ParseNode> for structuredmerge_core::parsed::ParseNode {
    fn from(val: ParseNode) -> Self {
        Self {
            id: val.id,
            kind: val.kind,
            native_type: val.native_type,
            role: val.role.into(),
            named: val.named,
            missing: val.missing,
            has_error: val.has_error,
            span: val.span.into(),
            parent_id: val.parent_id,
            children: val.children.into_iter().map(Into::into).collect(),
            semantic_roles: val.semantic_roles.into_iter().collect(),
            unsupported_features: val.unsupported_features.into_iter().collect(),
            extensions: val.extensions.into_iter().map(Into::into).collect(),
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
            extra: val
                .extra
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::parsed::ParseNode> for ParseNode {
    fn from(val: structuredmerge_core::parsed::ParseNode) -> Self {
        Self {
            id: val.id.to_string(),
            kind: val.kind.to_string(),
            native_type: val.native_type.to_string(),
            role: val.role.into(),
            named: val.named,
            missing: val.missing,
            has_error: val.has_error,
            span: val.span.into(),
            parent_id: val.parent_id.map(|v| v.to_string()),
            children: val.children.into_iter().map(Into::into).collect(),
            semantic_roles: val.semantic_roles.into_iter().collect(),
            unsupported_features: val.unsupported_features.into_iter().collect(),
            extensions: val.extensions.into_iter().map(Into::into).collect(),
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.to_string(), v.to_string()))
                .collect(),
            extra: val.extra.into_iter().map(|(k, v)| (k.to_string(), v.to_string())).collect(),
        }
    }
}

#[allow(clippy::needless_update)]
impl From<ParseOptions> for structuredmerge_core::service::ParseOptions {
    fn from(val: ParseOptions) -> Self {
        Self {
            comments: val.comments,
            tokens: val.tokens,
            diagnostics: val.diagnostics,
            native_extensions: val.native_extensions,
            ..Default::default()
        }
    }
}

impl From<structuredmerge_core::service::ParseOptions> for ParseOptions {
    fn from(val: structuredmerge_core::service::ParseOptions) -> Self {
        Self {
            comments: val.comments,
            tokens: val.tokens,
            diagnostics: val.diagnostics,
            native_extensions: val.native_extensions,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ParseOutput> for structuredmerge_core::parsed::ParseOutput {
    fn from(val: ParseOutput) -> Self {
        Self {
            request_id: val.request_id,
            source: val.source.into(),
            ok: val.ok,
            root_id: val.root_id,
            nodes: val.nodes.into_iter().map(Into::into).collect(),
            comments: val.comments.into_iter().map(Into::into).collect(),
            diagnostics: val.diagnostics.into_iter().map(Into::into).collect(),
            extensions: val.extensions.into_iter().map(Into::into).collect(),
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
            extra: val
                .extra
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::parsed::ParseOutput> for ParseOutput {
    fn from(val: structuredmerge_core::parsed::ParseOutput) -> Self {
        Self {
            request_id: val.request_id.to_string(),
            source: val.source.into(),
            ok: val.ok,
            root_id: val.root_id.map(|v| v.to_string()),
            nodes: val.nodes.into_iter().map(Into::into).collect(),
            comments: val.comments.into_iter().map(Into::into).collect(),
            diagnostics: val.diagnostics.into_iter().map(Into::into).collect(),
            extensions: val.extensions.into_iter().map(Into::into).collect(),
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.to_string(), v.to_string()))
                .collect(),
            extra: val.extra.into_iter().map(|(k, v)| (k.to_string(), v.to_string())).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ParseRequest> for structuredmerge_core::service::ParseRequest {
    fn from(val: ParseRequest) -> Self {
        Self {
            schema: val.schema,
            request_id: val.request_id,
            source: val.source.into(),
            language: val.language,
            dialect: val.dialect,
            selection: val.selection.into(),
            options: val.options.into(),
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
            extra: val
                .extra
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::service::ParseRequest> for ParseRequest {
    fn from(val: structuredmerge_core::service::ParseRequest) -> Self {
        Self {
            schema: val.schema.to_string(),
            request_id: val.request_id.to_string(),
            source: val.source.into(),
            language: val.language.to_string(),
            dialect: val.dialect.map(|v| v.to_string()),
            selection: val.selection.into(),
            options: val.options.into(),
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.to_string(), v.to_string()))
                .collect(),
            extra: val.extra.into_iter().map(|(k, v)| (k.to_string(), v.to_string())).collect(),
        }
    }
}

impl From<ParserCandidate> for structuredmerge_core::service::ParserCandidate {
    fn from(val: ParserCandidate) -> Self {
        Self {
            backend_id: val.backend_id,
            rejections: val.rejections.into_iter().collect(),
            available: val.available,
            loadable: val.loadable,
            probe_fault: val.probe_fault,
            request_preference: val.request_preference,
            profile_preference: val.profile_preference,
            priority: val.priority,
            selected: val.selected,
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<structuredmerge_core::service::ParserCandidate> for ParserCandidate {
    fn from(val: structuredmerge_core::service::ParserCandidate) -> Self {
        Self {
            backend_id: val.backend_id.to_string(),
            rejections: val.rejections.into_iter().collect(),
            available: val.available,
            loadable: val.loadable,
            probe_fault: val.probe_fault.map(|v| v.to_string()),
            request_preference: val.request_preference,
            profile_preference: val.profile_preference,
            priority: val.priority,
            selected: val.selected,
        }
    }
}

impl From<ParserProbeRequest> for structuredmerge_core::service::ParserProbeRequest {
    fn from(val: ParserProbeRequest) -> Self {
        Self { language: val.language, dialect: val.dialect }
    }
}

#[allow(clippy::redundant_closure)]
impl From<structuredmerge_core::service::ParserProbeRequest> for ParserProbeRequest {
    fn from(val: structuredmerge_core::service::ParserProbeRequest) -> Self {
        Self { language: val.language.to_string(), dialect: val.dialect.map(|v| v.to_string()) }
    }
}

impl From<ParserProbeResult> for structuredmerge_core::service::ParserProbeResult {
    fn from(val: ParserProbeResult) -> Self {
        Self { available: val.available, loadable: val.loadable }
    }
}

impl From<structuredmerge_core::service::ParserProbeResult> for ParserProbeResult {
    fn from(val: structuredmerge_core::service::ParserProbeResult) -> Self {
        Self { available: val.available, loadable: val.loadable }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<ParserProviderDescriptor> for structuredmerge_core::service::ParserProviderDescriptor {
    fn from(val: ParserProviderDescriptor) -> Self {
        Self {
            id: val.id,
            family: val.family,
            runtime: val.runtime,
            package: val.package,
            package_version: val.package_version,
            parser: val.parser,
            parser_version: val.parser_version,
            grammar: val.grammar,
            grammar_version: val.grammar_version,
            languages: val.languages.into_iter().collect(),
            dialects: val.dialects.into_iter().collect(),
            contracts: val.contracts.into_iter().collect(),
            capabilities: val.capabilities.into_iter().collect(),
            probe_id: val.probe_id,
            priority: val.priority,
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.into(), serde_json::from_str(&v).unwrap_or_default()))
                .collect(),
            extensions: val.extensions.into_iter().map(Into::into).collect(),
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::service::ParserProviderDescriptor> for ParserProviderDescriptor {
    fn from(val: structuredmerge_core::service::ParserProviderDescriptor) -> Self {
        Self {
            id: val.id.to_string(),
            family: val.family.to_string(),
            runtime: val.runtime.to_string(),
            package: val.package.to_string(),
            package_version: val.package_version.to_string(),
            parser: val.parser.to_string(),
            parser_version: val.parser_version.to_string(),
            grammar: val.grammar.map(|v| v.to_string()),
            grammar_version: val.grammar_version.map(|v| v.to_string()),
            languages: val.languages.into_iter().collect(),
            dialects: val.dialects.into_iter().collect(),
            contracts: val.contracts.into_iter().collect(),
            capabilities: val.capabilities.into_iter().collect(),
            probe_id: val.probe_id.to_string(),
            priority: val.priority,
            metadata: val
                .metadata
                .into_iter()
                .map(|(k, v)| (k.to_string(), v.to_string()))
                .collect(),
            extensions: val.extensions.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<ParserSelection> for structuredmerge_core::service::ParserSelection {
    fn from(val: ParserSelection) -> Self {
        Self {
            backend_id: val.backend_id,
            preference: val.preference.into_iter().collect(),
            required_capabilities: val.required_capabilities.into_iter().collect(),
        }
    }
}

#[allow(clippy::redundant_closure)]
impl From<structuredmerge_core::service::ParserSelection> for ParserSelection {
    fn from(val: structuredmerge_core::service::ParserSelection) -> Self {
        Self {
            backend_id: val.backend_id.map(|v| v.to_string()),
            preference: val.preference.into_iter().collect(),
            required_capabilities: val.required_capabilities.into_iter().collect(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<PolicyReference> for structuredmerge_core::PolicyReference {
    fn from(val: PolicyReference) -> Self {
        Self { surface: val.surface.into(), name: val.name }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::PolicyReference> for PolicyReference {
    fn from(val: structuredmerge_core::PolicyReference) -> Self {
        Self { surface: val.surface.into(), name: val.name.to_string() }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ProbeBatchRequest> for structuredmerge_core::ProbeBatchRequest {
    fn from(val: ProbeBatchRequest) -> Self {
        Self { items: val.items.into_iter().map(Into::into).collect() }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::ProbeBatchRequest> for ProbeBatchRequest {
    fn from(val: structuredmerge_core::ProbeBatchRequest) -> Self {
        Self { items: val.items.into_iter().map(Into::into).collect() }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ProbeBatchResult> for structuredmerge_core::ProbeBatchResult {
    fn from(val: ProbeBatchResult) -> Self {
        Self { items: val.items.into_iter().map(Into::into).collect() }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::ProbeBatchResult> for ProbeBatchResult {
    fn from(val: structuredmerge_core::ProbeBatchResult) -> Self {
        Self { items: val.items.into_iter().map(Into::into).collect() }
    }
}

#[allow(clippy::useless_conversion)]
impl From<RetainedSourceSegment> for structuredmerge_core::RetainedSourceSegment {
    fn from(val: RetainedSourceSegment) -> Self {
        Self {
            id: val.id,
            source_id: val.source_id,
            source_role: val.source_role.into(),
            source_range: val.source_range.into(),
            output_range: val.output_range.into(),
            sha256: val.sha256,
            owner_id: val.owner_id,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::RetainedSourceSegment> for RetainedSourceSegment {
    fn from(val: structuredmerge_core::RetainedSourceSegment) -> Self {
        Self {
            id: val.id.to_string(),
            source_id: val.source_id.to_string(),
            source_role: val.source_role.into(),
            source_range: val.source_range.into(),
            output_range: val.output_range.into(),
            sha256: val.sha256.to_string(),
            owner_id: val.owner_id.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<ReviewDiagnosticDetail> for structuredmerge_core::ReviewDiagnosticDetail {
    fn from(val: ReviewDiagnosticDetail) -> Self {
        Self {
            request_id: val.request_id,
            action: val.action.map(Into::into),
            reason: val.reason.map(Into::into),
            payload_kind: val.payload_kind,
            expected_family: val.expected_family,
            provided_family: val.provided_family,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::ReviewDiagnosticDetail> for ReviewDiagnosticDetail {
    fn from(val: structuredmerge_core::ReviewDiagnosticDetail) -> Self {
        Self {
            request_id: val.request_id.map(|v| v.to_string()),
            action: val.action.map(Into::into),
            reason: val.reason.map(Into::into),
            payload_kind: val.payload_kind.map(|v| v.to_string()),
            expected_family: val.expected_family.map(|v| v.to_string()),
            provided_family: val.provided_family.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<SelectionReport> for structuredmerge_core::service::SelectionReport {
    fn from(val: SelectionReport) -> Self {
        Self {
            requested: val.requested.into(),
            generation: val.generation,
            digest: val.digest,
            candidates: val.candidates.into_iter().map(Into::into).collect(),
            selected_backend: val.selected_backend,
        }
    }
}

#[allow(clippy::redundant_closure, clippy::useless_conversion)]
impl From<structuredmerge_core::service::SelectionReport> for SelectionReport {
    fn from(val: structuredmerge_core::service::SelectionReport) -> Self {
        Self {
            requested: val.requested.into(),
            generation: val.generation,
            digest: val.digest.to_string(),
            candidates: val.candidates.into_iter().map(Into::into).collect(),
            selected_backend: val.selected_backend.map(|v| v.to_string()),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<SourceDescriptor> for structuredmerge_core::source::SourceDescriptor {
    fn from(val: SourceDescriptor) -> Self {
        Self {
            source_id: val.source_id,
            role: val.role.into(),
            byte_length: val.byte_length,
            sha256: val.sha256,
            encoding: val.encoding.into(),
            bom: val.bom,
            line_endings: val.line_endings.into(),
            final_newline: val.final_newline,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::source::SourceDescriptor> for SourceDescriptor {
    fn from(val: structuredmerge_core::source::SourceDescriptor) -> Self {
        Self {
            source_id: val.source_id.to_string(),
            role: val.role.into(),
            byte_length: val.byte_length,
            sha256: val.sha256.to_string(),
            encoding: val.encoding.into(),
            bom: val.bom,
            line_endings: val.line_endings.into(),
            final_newline: val.final_newline,
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<SourceInput> for structuredmerge_core::source::SourceInput {
    fn from(val: SourceInput) -> Self {
        Self { descriptor: val.descriptor.into(), bytes: val.bytes.to_vec().into() }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::source::SourceInput> for SourceInput {
    fn from(val: structuredmerge_core::source::SourceInput) -> Self {
        Self { descriptor: val.descriptor.into(), bytes: val.bytes.to_vec().into() }
    }
}

impl From<SourcePoint> for structuredmerge_core::SourcePoint {
    fn from(val: SourcePoint) -> Self {
        Self { row: val.row, column: val.column }
    }
}

impl From<structuredmerge_core::SourcePoint> for SourcePoint {
    fn from(val: structuredmerge_core::SourcePoint) -> Self {
        Self { row: val.row, column: val.column }
    }
}

#[allow(clippy::useless_conversion)]
impl From<SourceSpan> for structuredmerge_core::SourceSpan {
    fn from(val: SourceSpan) -> Self {
        Self {
            range: val.range.into(),
            start_point: val.start_point.into(),
            end_point: val.end_point.into(),
        }
    }
}

#[allow(clippy::useless_conversion)]
impl From<structuredmerge_core::SourceSpan> for SourceSpan {
    fn from(val: structuredmerge_core::SourceSpan) -> Self {
        Self {
            range: val.range.into(),
            start_point: val.start_point.into(),
            end_point: val.end_point.into(),
        }
    }
}

impl From<AttachmentHint> for structuredmerge_core::parsed::AttachmentHint {
    fn from(val: AttachmentHint) -> Self {
        match val {
            AttachmentHint::Leading => Self::Leading,
            AttachmentHint::Inline => Self::Inline,
            AttachmentHint::Trailing => Self::Trailing,
            AttachmentHint::Preamble => Self::Preamble,
            AttachmentHint::Postlude => Self::Postlude,
            AttachmentHint::Orphan => Self::Orphan,
            AttachmentHint::Unknown => Self::Unknown,
        }
    }
}

impl From<structuredmerge_core::parsed::AttachmentHint> for AttachmentHint {
    fn from(val: structuredmerge_core::parsed::AttachmentHint) -> Self {
        match val {
            structuredmerge_core::parsed::AttachmentHint::Leading => Self::Leading,
            structuredmerge_core::parsed::AttachmentHint::Inline => Self::Inline,
            structuredmerge_core::parsed::AttachmentHint::Trailing => Self::Trailing,
            structuredmerge_core::parsed::AttachmentHint::Preamble => Self::Preamble,
            structuredmerge_core::parsed::AttachmentHint::Postlude => Self::Postlude,
            structuredmerge_core::parsed::AttachmentHint::Orphan => Self::Orphan,
            structuredmerge_core::parsed::AttachmentHint::Unknown => Self::Unknown,
        }
    }
}

impl From<ConflictAlternativeState> for structuredmerge_core::ConflictAlternativeState {
    fn from(val: ConflictAlternativeState) -> Self {
        match val {
            ConflictAlternativeState::Present => Self::Present,
            ConflictAlternativeState::Absent => Self::Absent,
        }
    }
}

impl From<structuredmerge_core::ConflictAlternativeState> for ConflictAlternativeState {
    fn from(val: structuredmerge_core::ConflictAlternativeState) -> Self {
        match val {
            structuredmerge_core::ConflictAlternativeState::Present => Self::Present,
            structuredmerge_core::ConflictAlternativeState::Absent => Self::Absent,
        }
    }
}

impl From<DiagnosticCategory> for structuredmerge_core::DiagnosticCategory {
    fn from(val: DiagnosticCategory) -> Self {
        match val {
            DiagnosticCategory::ParseError => Self::ParseError,
            DiagnosticCategory::DestinationParseError => Self::DestinationParseError,
            DiagnosticCategory::UnsupportedFeature => Self::UnsupportedFeature,
            DiagnosticCategory::FallbackApplied => Self::FallbackApplied,
            DiagnosticCategory::Ambiguity => Self::Ambiguity,
            DiagnosticCategory::MergeConflict => Self::MergeConflict,
            DiagnosticCategory::KindMismatch => Self::KindMismatch,
            DiagnosticCategory::UnsupportedVersion => Self::UnsupportedVersion,
            DiagnosticCategory::AssumedDefault => Self::AssumedDefault,
            DiagnosticCategory::ConfigurationError => Self::ConfigurationError,
            DiagnosticCategory::ReplayRejected => Self::ReplayRejected,
        }
    }
}

impl From<structuredmerge_core::DiagnosticCategory> for DiagnosticCategory {
    fn from(val: structuredmerge_core::DiagnosticCategory) -> Self {
        match val {
            structuredmerge_core::DiagnosticCategory::ParseError => Self::ParseError,
            structuredmerge_core::DiagnosticCategory::DestinationParseError => {
                Self::DestinationParseError
            }
            structuredmerge_core::DiagnosticCategory::UnsupportedFeature => {
                Self::UnsupportedFeature
            }
            structuredmerge_core::DiagnosticCategory::FallbackApplied => Self::FallbackApplied,
            structuredmerge_core::DiagnosticCategory::Ambiguity => Self::Ambiguity,
            structuredmerge_core::DiagnosticCategory::MergeConflict => Self::MergeConflict,
            structuredmerge_core::DiagnosticCategory::KindMismatch => Self::KindMismatch,
            structuredmerge_core::DiagnosticCategory::UnsupportedVersion => {
                Self::UnsupportedVersion
            }
            structuredmerge_core::DiagnosticCategory::AssumedDefault => Self::AssumedDefault,
            structuredmerge_core::DiagnosticCategory::ConfigurationError => {
                Self::ConfigurationError
            }
            structuredmerge_core::DiagnosticCategory::ReplayRejected => Self::ReplayRejected,
        }
    }
}

impl From<DiagnosticSeverity> for structuredmerge_core::DiagnosticSeverity {
    fn from(val: DiagnosticSeverity) -> Self {
        match val {
            DiagnosticSeverity::Info => Self::Info,
            DiagnosticSeverity::Warning => Self::Warning,
            DiagnosticSeverity::Error => Self::Error,
        }
    }
}

impl From<structuredmerge_core::DiagnosticSeverity> for DiagnosticSeverity {
    fn from(val: structuredmerge_core::DiagnosticSeverity) -> Self {
        match val {
            structuredmerge_core::DiagnosticSeverity::Info => Self::Info,
            structuredmerge_core::DiagnosticSeverity::Warning => Self::Warning,
            structuredmerge_core::DiagnosticSeverity::Error => Self::Error,
        }
    }
}

impl From<NodeRole> for structuredmerge_core::NodeRole {
    fn from(val: NodeRole) -> Self {
        match val {
            NodeRole::Structural => Self::Structural,
            NodeRole::Token => Self::Token,
            NodeRole::Trivia => Self::Trivia,
            NodeRole::Comment => Self::Comment,
            NodeRole::Delimiter => Self::Delimiter,
            NodeRole::Separator => Self::Separator,
            NodeRole::Virtual => Self::Virtual,
            NodeRole::Error => Self::Error,
            NodeRole::Opaque => Self::Opaque,
        }
    }
}

impl From<structuredmerge_core::NodeRole> for NodeRole {
    fn from(val: structuredmerge_core::NodeRole) -> Self {
        match val {
            structuredmerge_core::NodeRole::Structural => Self::Structural,
            structuredmerge_core::NodeRole::Token => Self::Token,
            structuredmerge_core::NodeRole::Trivia => Self::Trivia,
            structuredmerge_core::NodeRole::Comment => Self::Comment,
            structuredmerge_core::NodeRole::Delimiter => Self::Delimiter,
            structuredmerge_core::NodeRole::Separator => Self::Separator,
            structuredmerge_core::NodeRole::Virtual => Self::Virtual,
            structuredmerge_core::NodeRole::Error => Self::Error,
            structuredmerge_core::NodeRole::Opaque => Self::Opaque,
        }
    }
}

impl From<ParseSeverity> for structuredmerge_core::parsed::ParseSeverity {
    fn from(val: ParseSeverity) -> Self {
        match val {
            ParseSeverity::Info => Self::Info,
            ParseSeverity::Warning => Self::Warning,
            ParseSeverity::Error => Self::Error,
        }
    }
}

impl From<structuredmerge_core::parsed::ParseSeverity> for ParseSeverity {
    fn from(val: structuredmerge_core::parsed::ParseSeverity) -> Self {
        match val {
            structuredmerge_core::parsed::ParseSeverity::Info => Self::Info,
            structuredmerge_core::parsed::ParseSeverity::Warning => Self::Warning,
            structuredmerge_core::parsed::ParseSeverity::Error => Self::Error,
        }
    }
}

impl From<PolicySurface> for structuredmerge_core::PolicySurface {
    fn from(val: PolicySurface) -> Self {
        match val {
            PolicySurface::Fallback => Self::Fallback,
            PolicySurface::Array => Self::Array,
        }
    }
}

impl From<structuredmerge_core::PolicySurface> for PolicySurface {
    fn from(val: structuredmerge_core::PolicySurface) -> Self {
        match val {
            structuredmerge_core::PolicySurface::Fallback => Self::Fallback,
            structuredmerge_core::PolicySurface::Array => Self::Array,
        }
    }
}

impl From<ReviewDecisionAction> for structuredmerge_core::ReviewDecisionAction {
    fn from(val: ReviewDecisionAction) -> Self {
        match val {
            ReviewDecisionAction::AcceptDefaultContext => Self::AcceptDefaultContext,
            ReviewDecisionAction::ProvideExplicitContext => Self::ProvideExplicitContext,
            ReviewDecisionAction::ApplyDelegatedChildGroup => Self::ApplyDelegatedChildGroup,
        }
    }
}

impl From<structuredmerge_core::ReviewDecisionAction> for ReviewDecisionAction {
    fn from(val: structuredmerge_core::ReviewDecisionAction) -> Self {
        match val {
            structuredmerge_core::ReviewDecisionAction::AcceptDefaultContext => {
                Self::AcceptDefaultContext
            }
            structuredmerge_core::ReviewDecisionAction::ProvideExplicitContext => {
                Self::ProvideExplicitContext
            }
            structuredmerge_core::ReviewDecisionAction::ApplyDelegatedChildGroup => {
                Self::ApplyDelegatedChildGroup
            }
        }
    }
}

impl From<ReviewDiagnosticReason> for structuredmerge_core::ReviewDiagnosticReason {
    fn from(val: ReviewDiagnosticReason) -> Self {
        match val {
            ReviewDiagnosticReason::MissingRequiredPayload => Self::MissingRequiredPayload,
            ReviewDiagnosticReason::FamilyMismatch => Self::FamilyMismatch,
            ReviewDiagnosticReason::RequestNotFound => Self::RequestNotFound,
        }
    }
}

impl From<structuredmerge_core::ReviewDiagnosticReason> for ReviewDiagnosticReason {
    fn from(val: structuredmerge_core::ReviewDiagnosticReason) -> Self {
        match val {
            structuredmerge_core::ReviewDiagnosticReason::MissingRequiredPayload => {
                Self::MissingRequiredPayload
            }
            structuredmerge_core::ReviewDiagnosticReason::FamilyMismatch => Self::FamilyMismatch,
            structuredmerge_core::ReviewDiagnosticReason::RequestNotFound => Self::RequestNotFound,
        }
    }
}

impl From<SourceEncoding> for structuredmerge_core::source::SourceEncoding {
    fn from(val: SourceEncoding) -> Self {
        match val {
            SourceEncoding::Utf8 => Self::Utf8,
            SourceEncoding::Binary => Self::Binary,
        }
    }
}

impl From<structuredmerge_core::source::SourceEncoding> for SourceEncoding {
    fn from(val: structuredmerge_core::source::SourceEncoding) -> Self {
        match val {
            structuredmerge_core::source::SourceEncoding::Utf8 => Self::Utf8,
            structuredmerge_core::source::SourceEncoding::Binary => Self::Binary,
        }
    }
}

impl From<SourceRevision> for structuredmerge_core::SourceRevision {
    fn from(val: SourceRevision) -> Self {
        match val {
            SourceRevision::Base => Self::Base,
            SourceRevision::Ours => Self::Ours,
            SourceRevision::Theirs => Self::Theirs,
        }
    }
}

impl From<structuredmerge_core::SourceRevision> for SourceRevision {
    fn from(val: structuredmerge_core::SourceRevision) -> Self {
        match val {
            structuredmerge_core::SourceRevision::Base => Self::Base,
            structuredmerge_core::SourceRevision::Ours => Self::Ours,
            structuredmerge_core::SourceRevision::Theirs => Self::Theirs,
        }
    }
}

impl From<SourceRole> for structuredmerge_core::source::SourceRole {
    fn from(val: SourceRole) -> Self {
        match val {
            SourceRole::Source => Self::Source,
            SourceRole::Before => Self::Before,
            SourceRole::After => Self::After,
            SourceRole::Incoming => Self::Incoming,
            SourceRole::Current => Self::Current,
            SourceRole::Base => Self::Base,
            SourceRole::Ours => Self::Ours,
            SourceRole::Theirs => Self::Theirs,
            SourceRole::Output => Self::Output,
        }
    }
}

impl From<structuredmerge_core::source::SourceRole> for SourceRole {
    fn from(val: structuredmerge_core::source::SourceRole) -> Self {
        match val {
            structuredmerge_core::source::SourceRole::Source => Self::Source,
            structuredmerge_core::source::SourceRole::Before => Self::Before,
            structuredmerge_core::source::SourceRole::After => Self::After,
            structuredmerge_core::source::SourceRole::Incoming => Self::Incoming,
            structuredmerge_core::source::SourceRole::Current => Self::Current,
            structuredmerge_core::source::SourceRole::Base => Self::Base,
            structuredmerge_core::source::SourceRole::Ours => Self::Ours,
            structuredmerge_core::source::SourceRole::Theirs => Self::Theirs,
            structuredmerge_core::source::SourceRole::Output => Self::Output,
        }
    }
}

impl From<ThreeWayMergeOutcome> for structuredmerge_core::ThreeWayMergeOutcome {
    fn from(val: ThreeWayMergeOutcome) -> Self {
        match val {
            ThreeWayMergeOutcome::Clean => Self::Clean,
            ThreeWayMergeOutcome::Conflict => Self::Conflict,
            ThreeWayMergeOutcome::Error => Self::Error,
        }
    }
}

impl From<structuredmerge_core::ThreeWayMergeOutcome> for ThreeWayMergeOutcome {
    fn from(val: structuredmerge_core::ThreeWayMergeOutcome) -> Self {
        match val {
            structuredmerge_core::ThreeWayMergeOutcome::Clean => Self::Clean,
            structuredmerge_core::ThreeWayMergeOutcome::Conflict => Self::Conflict,
            structuredmerge_core::ThreeWayMergeOutcome::Error => Self::Error,
        }
    }
}

#[pymodule]
pub fn _native(m: &Bound<'_, PyModule>) -> PyResult<()> {
    m.add_class::<ByteRange>()?;
    m.add_class::<ChildEdge>()?;
    m.add_class::<ConflictAlternative>()?;
    m.add_class::<CoreError>()?;
    m.add_class::<CoreParseResult>()?;
    m.add_class::<Diagnostic>()?;
    m.add_class::<LineEndings>()?;
    m.add_class::<MergeConflict>()?;
    m.add_class::<NativeExtension>()?;
    m.add_class::<NativeMergeResult>()?;
    m.add_class::<OwnedSourceRegion>()?;
    m.add_class::<ParseBatchRequest>()?;
    m.add_class::<ParseBatchResult>()?;
    m.add_class::<ParseComment>()?;
    m.add_class::<ParseDiagnostic>()?;
    m.add_class::<ParseLimits>()?;
    m.add_class::<ParseNode>()?;
    m.add_class::<ParseOptions>()?;
    m.add_class::<ParseOutput>()?;
    m.add_class::<ParseRequest>()?;
    m.add_class::<ParserCandidate>()?;
    m.add_class::<ParserProbeRequest>()?;
    m.add_class::<ParserProbeResult>()?;
    m.add_class::<ParserProviderDescriptor>()?;
    m.add_class::<ParserSelection>()?;
    m.add_class::<PolicyReference>()?;
    m.add_class::<ProbeBatchRequest>()?;
    m.add_class::<ProbeBatchResult>()?;
    m.add_class::<RetainedSourceSegment>()?;
    m.add_class::<ReviewDiagnosticDetail>()?;
    m.add_class::<SelectionReport>()?;
    m.add_class::<SourceDescriptor>()?;
    m.add_class::<SourceInput>()?;
    m.add_class::<SourcePoint>()?;
    m.add_class::<SourceSpan>()?;
    m.add_class::<AttachmentHint>()?;
    m.add_class::<ConflictAlternativeState>()?;
    m.add_class::<DiagnosticCategory>()?;
    m.add_class::<DiagnosticSeverity>()?;
    m.add_class::<NodeRole>()?;
    m.add_class::<ParseSeverity>()?;
    m.add_class::<PolicySurface>()?;
    m.add_class::<ReviewDecisionAction>()?;
    m.add_class::<ReviewDiagnosticReason>()?;
    m.add_class::<SourceEncoding>()?;
    m.add_class::<SourceRevision>()?;
    m.add_class::<SourceRole>()?;
    m.add_class::<ThreeWayMergeOutcome>()?;
    m.add_class::<PyParserHostMarker>()?;
    m.add_function(wrap_pyfunction!(merge_python_declarations, m)?)?;
    m.add_function(wrap_pyfunction!(merge_yaml_mapping, m)?)?;
    m.add_function(wrap_pyfunction!(parse_sources, m)?)?;
    m.add_function(wrap_pyfunction!(register_parser_host, m)?)?;
    m.add_function(wrap_pyfunction!(unregister_parser_host, m)?)?;
    Ok(())
}
